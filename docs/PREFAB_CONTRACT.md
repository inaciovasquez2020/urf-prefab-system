# URF Prefab Contract

A prefab is a canonical, versioned, invariant-preserving bundle
used to compose URF-certified systems.

Requirements:
- Declared invariants
- Deterministic structure
- Versioned outputs
- Verifier compatibility

Prefabs do not introduce new semantics.
