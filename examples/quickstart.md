# Prefab Quickstart

1. Clone `urf-spine`, `urf-core`, `chronos-urf-rr`, `cvre`.
2. From `urf-prefab-system`:
```sh
./urf-llm-check --emit-cert path/to/input
3. Inspect generated certificates in output/. 4.
Verify with CVRE or downstream verifier.
